import React from 'react';

const Job = ({ job }) => {
    const { id, title, name, logo, Contact_information, loction, salary } = job;
    return (
        <div>
            
        </div>
    );
};

export default Job;